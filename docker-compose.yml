services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: signalforge
      POSTGRES_PASSWORD: signalforge
      POSTGRES_DB: signalforge
    ports:
      - "5432:5432"
    volumes:
      - signalforge-postgres:/var/lib/postgresql/data

volumes:
  signalforge-postgres:
